## C078
```
IF <Destination Type Code> is "Destination - Tax warehouse"
   THEN
     <Street Name> is 'O'
     <Postcode> is 'O'
     <City> is 'O'
   ELSE
     <Street Name> is 'R'
     <Postcode> is 'R'
     <City> is 'R'
```
