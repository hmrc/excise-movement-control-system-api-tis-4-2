## C101
```
IF <Trader Excise Number> is given
THEN
  <Trader Name> is 'O'
  <Street Name> is 'O'
  <Postcode> is 'O'
  <City> is 'O'
ELSE
  <Trader Name> is 'R'
  <Street Name> is 'R'
  <Postcode> is 'R'
  <City> is 'R'
```
